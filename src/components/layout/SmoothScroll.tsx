"use client";

import { ReactNode } from "react";

/** Native browser scroll — Lenis made wheel scrolling feel laggy. */
export function SmoothScroll({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
